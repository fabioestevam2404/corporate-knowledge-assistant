name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      db:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: cka
          POSTGRES_PASSWORD: cka
          # Named cka_test (not cka) so tests/conftest.py's database-name
          # redirect (see _test_database_url) is a no-op here — this whole
          # database is thrown away after the job either way, but staying
          # consistent avoids a CI-only special case.
          POSTGRES_DB: cka_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U cka -d cka_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql+psycopg://cka:cka@localhost:5432/cka_test
      ENVIRONMENT: test
      OTEL_TRACES_ENABLED: "false"

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "0.12.5"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --frozen

      - name: Ruff (lint)
        run: uv run ruff check .

      - name: Ruff (format check)
        run: uv run ruff format --check .

      - name: Type check (mypy)
        run: uv run mypy src tests scripts

      - name: Enable pgvector extension
        run: |
          docker exec $(docker ps -q --filter "ancestor=pgvector/pgvector:pg16") \
            psql -U cka -d cka_test -c "CREATE EXTENSION IF NOT EXISTS vector;"

      - name: Run migrations
        run: uv run alembic upgrade head

      - name: Unit tests (no external services required)
        run: uv run pytest tests/unit -q

      - name: Integration, security & observability tests
        run: >
          uv run pytest tests/integration tests/security tests/observability
          --cov=src/cka --cov-report=term-missing --cov-fail-under=80
